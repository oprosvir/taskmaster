#!/usr/bin/env python3
import argparse
import sys
from pathlib import Path

from config import ConfigError, ConfigNotFoundError, PrivilegeError
from core import TaskmasterDaemon
from common.exit_codes import EXIT_CONFIG_ERROR, EXIT_PRIVILEGE_ERROR, EXIT_OK


def get_config_path() -> Path:
    """
    Parse the config path from command-line arguments.

    Returns:
        Path: Path to the config file.
    """
    parser = argparse.ArgumentParser(description="Taskmaster process supervisor")
    parser.add_argument(
        "-c",
        "--config",
        type=Path,
        default=Path("config.toml"),
        help="Path to the configuration file (default: ./config.toml)",
    )
    args = parser.parse_args()
    if not args.config.is_file():
        raise ConfigNotFoundError(f"configuration file '{args.config}' not found")
    return args.config


def _fail(message: str, code: int) -> int:
    print(f"[taskmasterd] {message}", file=sys.stderr)
    return code


def main() -> int:
    try:
        config_path = get_config_path()
    except ConfigNotFoundError as e:
        return _fail(str(e), EXIT_CONFIG_ERROR)

    daemon = TaskmasterDaemon(config_path)

    try:
        daemon.reload_config()
    except ConfigError as e:
        return _fail(str(e), EXIT_CONFIG_ERROR)

    try:
        daemon.drop_privileges_if_root()
    except PrivilegeError as e:
        return _fail(str(e), EXIT_PRIVILEGE_ERROR)

    daemon.run()
    return EXIT_OK


if __name__ == "__main__":
    sys.exit(main())
